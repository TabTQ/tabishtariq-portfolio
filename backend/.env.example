# Postgres connection (all objects live inside POSTGRES_SCHEMA; no other schema is touched)
POSTGRES_HOST=192.168.10.198
POSTGRES_PORT=5432
POSTGRES_DB=webapps.test
POSTGRES_USER=dev-user
POSTGRES_PASSWORD=changeme
POSTGRES_SCHEMA=portfolio

# Comma-separated list of allowed frontend origins
CORS_ORIGINS=http://localhost:3000

# Admin session auth
JWT_SECRET=your-long-random-secret-key-here
JWT_EXPIRE_MINUTES=720
ADMIN_COOKIE_NAME=portfolio_admin

# Used by scripts/seed_data.py to (re)create the admin UI login user
ADMIN_USERNAME=postgres
ADMIN_PASSWORD=changeme
